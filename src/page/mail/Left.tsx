import styled from "styled-components";

const SidebarContainer = styled.div`
    display: flex;
    flex-direction: column;
`
export const Left = () => {
    return <div>
        <SidebarContainer/>
    </div>
}